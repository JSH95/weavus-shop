<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weavus.weavusshoppingmall.repo.UserRepository">
    <select id="findById" resultType="com.weavus.weavusshoppingmall.entity.User">
        select id, password, user_email as userEmail, card_number as cardNumber, card_cvc as cardCvc,
               user_name as userName, user_en_name as userEnName, created_date as createdDate
        from user
        where id = #{id}
    </select>


</mapper>