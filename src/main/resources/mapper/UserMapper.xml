<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weavus.weavusshoppingmall.repository.UserMapper">

    <select id="findById" resultType="com.weavus.weavusshoppingmall.entity.User">
        select * from user where id = #{id}
    </select>

    <select id="loginCheck" resultType="com.weavus.weavusshoppingmall.entity.User">
        select * from user where id = #{id} AND password = #{password}
    </select>

    <insert id="save">
        INSERT INTO user (id, password, user_email, card_number, card_cvc, user_name, user_en_name)
        VALUES (
                #{id},
                #{password},
                #{userEmail},
                #{cardNumber},
                #{cardCvc},
                #{userName},
                #{userEnName}
               );
    </insert>

</mapper>